{{- if eq .Values.orchestrator.enabled true -}}
kind: DeploymentConfig
apiVersion: apps.openshift.io/v1
metadata:
  name: {{ include "orc.fullname" . }}
  labels:
    {{- include "orc.labels" . | nindent 4 }}
  annotations:
    description: Defines how to deploy the application server
    template.alpha.openshift.io/wait-for-ready: 'true'
spec:
  strategy:
    type: Rolling
  triggers:
  - type: ImageChange
    imageChangeParams:
      automatic: true
      containerNames:
      - {{ include "orc.name" . }}
      from:
        kind: ImageStreamTag
        name: "{{ include "orc.name" . }}:latest"
  - type: ConfigChange
  replicas: 1
  selector:
    {{- include "orc.selectorLabels" . | nindent 4 }}
  template:
    metadata:
      labels:
        {{- include "orc.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - name: {{ include "orc.name" . }}
        image: "{{ .Values.orchestrator.image_repository }}/{{ .Values.namespace }}/{{ include "orc.name" . }}:latest"
        ports:
        - containerPort: 5009
        readinessProbe:
          timeoutSeconds: 3
          initialDelaySeconds: 3
          httpGet:
            path: "/"
            port: 5009
        livenessProbe:
          timeoutSeconds: 3
          initialDelaySeconds: 30
          httpGet:
            path: "/"
            port: 5009
        resources:
          limits:
            memory: "512Mi"
        env:
            - name: NLTK_DATA
              value: /usr/src/WHOA-FAQ-Answer-Project/nltk-data
            - name: SOLR_PORT
              value: 8983
            - name: SOLR_HOST
              value: {{ include "orc.fullname" . }}
{{- end }}