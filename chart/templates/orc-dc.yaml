{{- if eq .Values.orchestrator.enabled true -}}
kind: DeploymentConfig
apiVersion: apps.openshift.io/v1
metadata:
  name: "{{ template "orc.fullname" . }}"
  {{- if .Values.labels_enabled }}
  labels:
    {{- include "orc.labels" . | nindent 4 }}
  {{- end }}
  annotations:
    description: Defines how to deploy the application server
    template.alpha.openshift.io/wait-for-ready: 'true'
spec:
  strategy:
    type: Rolling
  triggers:
  - type: ImageChange
    imageChangeParams:
      automatic: true
      containerNames:
      - {{ template "orc.name" . }}
      from:
        kind: ImageStreamTag
        name: "{{ template "orc.name" . }}:latest"
  - type: ConfigChange
  replicas: 1
  selector:
    {{- include 'orc'.selectorLabels" . | nindent 4 }}
  template:
    metadata:
      labels:
        {{- include "orc.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - name: {{ template "orc.name" . }}
        image: "{{ .Values.orchestrator.image_repository }}/{{ .Values.namespace }}/{{ template "orc.name" . }}:latest"
        ports:
        - containerPort: 5009
        readinessProbe:
          timeoutSeconds: 3
          initialDelaySeconds: 3
          httpGet:
            path: "/"
            port: 5009
        livenessProbe:
          timeoutSeconds: 3
          initialDelaySeconds: 30
          httpGet:
            path: "/"
            port: 5009
        resources:
          limits:
            memory: "512Mi"
        env:
            - name: NLTK_DATA
              value: /usr/src/WHOA-FAQ-Answer-Project/nltk-data
{{- end }}