apiVersion: apps.openshift.io/v1
kind: DeploymentConfig
metadata:
  name: {{ .Values.botpress.name }}
spec:
  selector:
    app: {{ .Values.botpress.name }}
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ .Values.botpress.name }}
    spec:
      containers:
        - name: {{ .Values.botpress.name }}
          image: quay.io/whoacademy/botpress-team-jarvis
          ports:
            - containerPort: 3000
          env:
            - name: APP_DATA_PATH
              value: /temp