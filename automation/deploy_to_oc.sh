# Orchestrator

    # Create build config with current git url

    # create deployement 
    oc deploy --latest dc/<name>

    # Create servie

    # Create route

# Orchestrator Cache

    # Create build config with current git url

    # create deployement 
    oc deploy --latest dc/<name>

    # Create servie

    # Create route

# Botpresss

    # create deployement 
    oc deploy --latest dc/<name>

    # Create servie

    # Create route

